name: Build ZMK firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: rloutier/zmk/.github/workflows/build-user-config.yml@aekeynox
    with:
      archive_name: firmware-selenium
